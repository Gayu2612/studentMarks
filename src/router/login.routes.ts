import { Router } from 'express';
// import { loginEmail } from 'src/controller/login.controller';

const router: Router = Router();

// router.post('/loginMail', loginEmail)




export default router;

